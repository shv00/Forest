using UnityEngine;
using TheForest.Utils;

namespace Forest.Player
{
    public class OneHit : EnemyHealth
    {/*
        public new void Hit(int damage)
        {

            if (Menu.OneHit)
            {
                HitReal(damage = 1000);
            } else
            {
                HitReal(damage);
            }
        }
        */
    }
    
}
